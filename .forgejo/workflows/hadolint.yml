on:
  pull_request:
  push:
    branches: [main]

jobs:
  hadolint:
    runs-on:
      - ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: hadolint/hadolint-action@master
      - name: Run hadolint
        run: hadolint Dockerfile
